# fleet.yaml — Fleet GitOps bundle definition
# Documentation: https://fleet.rancher.io/ref-fleet-yaml
#
# Deploys the alien-geeko Helm chart across the demo fleet.
# Each target overrides clusterName so the Nostromo UI shows the right label.
# Clusters without demo=true match nothing and receive nothing.

defaultNamespace: alien-geeko

helm:
  chart: ./chart/alien-geeko
  takeOwnership: true

targets:
  # Vessel Alpha — Raspberry Pi 4, SL Micro 6.2, provisioned via EIB
  - name: vessel-alpha-pi4-eib
    clusterSelector:
      matchLabels:
        demo: "true"
        edge-type: pi-cluster
        kubernetes.io/arch: arm64
    helm:
      values:
        clusterName: "PI-CLUSTER-ARM64"

  # Vessel Beta — x86 NUC, provisioned via Elemental phone-home
  - name: vessel-beta-x86-elemental
    clusterSelector:
      matchLabels:
        demo: "true"
        edge-type: x86-elemental
    helm:
      values:
        clusterName: "VESSEL-BETA-ELEMENTAL"

  # Vessel Delta-k3s — Raspberry Pi 5, openSUSE Tumbleweed + k3s, Rancher Import
  - name: vessel-delta-pi5-k3s
    clusterSelector:
      matchLabels:
        demo: "true"
        edge-type: pi5-k3s
        kubernetes.io/arch: arm64
    helm:
      values:
        clusterName: "VESSEL-DELTA-K3S"

  # Vessel Delta-RKE2 — Raspberry Pi 5, openSUSE Tumbleweed + RKE2, Rancher Import
  - name: vessel-delta-pi5-rke2
    clusterSelector:
      matchLabels:
        demo: "true"
        edge-type: pi5-rke2
        kubernetes.io/arch: arm64
    helm:
      values:
        clusterName: "VESSEL-DELTA-RKE2"

  # Mission Control — x86 NUC running Rancher + Fleet + Elemental
  - name: mission-control-x86
    clusterSelector:
      matchLabels:
        demo: "true"
        edge-type: x86-cluster
    helm:
      values:
        clusterName: "X86-EDGE-NODE"

  # Fallback — any demo=true cluster that did not match a specific target above
  - name: all-demo-clusters
    clusterSelector:
      matchLabels:
        demo: "true"
    helm:
      values:
        clusterName: "EDGE-CLUSTER"
